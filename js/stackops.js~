function StackOps()
{
    this.stack = new Array();

    this.pop = function() {
        return this.stack.pop();
    }

    this.push = function(item) {
        this.stack.push(item);
    }

    this.length = function() {
        return this.stack.length;
    }

    this.Dup = function() {
        if(this.stack.length > 0) {
            this.stack.push(this.stack[this.stack.length]);
        }
    }

    this.Drop=function(item){
        if(this.stack.length > 0) {
            this.stack.pop();
        }
    }

    this.Swap = function() {
        var temp = this.stack.length;
        
        
    }

    this.Over = function() {
        
    }

    this.Rot = function() {
        
    }
}

$(function() {

alert(1);

var test = new StackOps();
test.push(1);
alert("stack length= " + test.length());

});
